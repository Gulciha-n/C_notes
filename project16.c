#include <stdio.h>
#include <math.h>

int main(){ 

    //MATH FONCTIONS IN C PROGRAMMING

    double A = sqrt(9);
    double B = pow(2, 4);
    int C = round(3.14);   // avarege value of a number
    int D = ceil(3.14);    // cealing value of a number 
    int E = floor(3.99);   // floor value of a number
    double F = fabs(-100); //absolute value of a number 
    double G = log(3);
    double H = sin(45);
    double I = cos(45);
    double J = tan(45);
    
    printf("\n%lf", G);

    return 0;
}
